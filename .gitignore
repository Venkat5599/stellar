# node / bun
node_modules/
bun.lockb

# rust / soroban
target/
**/target/

# circuit build artifacts (large)
circuits/build/*.zkey
circuits/build/*.ptau
circuits/build/*.wasm
circuits/build/*.r1cs
circuits/build/*.sym
circuits/build/*_js/
circuits/build/witness.wtns

# generated proofs / inputs (keep examples small if needed)
circuits/build/proof.json
circuits/build/public.json
circuits/build/input.json

# secrets — NEVER commit
.env
.env.*
!.env.example
!**/.env.example
*.secret
identity*.toml
*_seed.txt
# generated build artifacts that embed testnet private keys
sdk/build/veil_manifest.json
sdk/build/agent_fabric.json

# os
.DS_Store
Thumbs.db

.playwright-mcp/

# transient agent prover scratch
circuits/build/agent_insert_*.json
sdk/build/veil_contract.txt
